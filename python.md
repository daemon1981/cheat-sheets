Work with python env (python 3 required)
```
python -m venv .venv
.\.venv\Scripts\activate # Windows
source .venv/bin/activate # Linux
pip install -e .
.\.venv\Scripts\activate # Windows
source .venv/bin/activate # Linux
```
